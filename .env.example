# LLM Council Docker Configuration
# Copy this file to .env and fill in your values

# Required: Your OpenRouter API key
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Optional: Frontend API URL (default: /api for production with nginx proxy)
# For development without Docker: http://localhost:8001
# For production with nginx proxy: /api (uses relative path)
VITE_API_URL=/api

# Optional: CORS origins for backend (comma-separated)
# Set this to your domain in production
# Example: https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGINS=http://localhost,http://localhost:80
